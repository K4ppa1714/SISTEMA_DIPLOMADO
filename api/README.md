# api/ — FastAPI en Vercel (Python)

Dueño: **Claude-A** (T-10, T-11, T-12, T-15), salvo `_lib/tarifas.py` (**Claude-E**, T-03).
Endpoints: `/api/salud`, `/api/rag`, `/api/triage`, `/api/agente`, `/api/importe` (docs/CONTRATOS.md §3).
